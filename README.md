# Working-with-Json
This project demonstrates how Python can be used to extract JSON data from GitHub and APIs, convert the data into Pandas DataFrames, and save the processed data into CSV files for analysis.
